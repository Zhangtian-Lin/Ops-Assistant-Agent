# Windows 性能变慢排查（Demo SOP）

## 初步判断

用户描述“电脑卡、响应慢、程序打开慢”时，先读取 CPU、内存、磁盘可用空间和进程情况。不要直接结束进程或清理文件。CPU 持续高于 85% 说明应继续定位占用最高的进程；内存可用量很低时优先检查内存压力。

## 磁盘与启动项

系统盘剩余空间不足 15% 会影响更新、临时文件和虚拟内存。检查系统盘空间、计划任务和启动项；先记录证据，再由用户决定是否处理。性能检查是只读诊断，不执行优化工具或注册表修改。
