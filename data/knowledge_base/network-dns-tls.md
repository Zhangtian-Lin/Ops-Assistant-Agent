# 网络、DNS 与 TLS 排查（Demo SOP）

## 本机网络状态

先查看网卡、IP 地址、默认网关、DNS 服务器、路由和防火墙状态。此步骤只读取本机配置，不修改 DNS、代理或路由。

## 受控外部检查

DNS、TCP 和 TLS 检查只允许策略白名单内的主机和端口。DNS 失败时检查解析结果；TCP 失败时检查端口和网络路径；TLS 检查关注证书有效期和主机名匹配。禁止把诊断接口变成任意网段端口扫描。
