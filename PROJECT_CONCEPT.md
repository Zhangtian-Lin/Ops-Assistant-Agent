# 项目概念

## Agent基础实现 

- `route_task()` / `parse_action_and_object()`
  - 将用户自然语言请求解析为工具调用意图。
- 低风险工具执行模块
  - `check_cpu`
  - `check_memory`
  - `check_disk`
  - `check_service`
  - `search_files`
- 参数校验与安全检查
  - 工具参数过滤
  - 检查服务名白名单
  - 磁盘路径安全校验
- 高风险工具审批机制雏形
  - `DANGEROUS_TOOLS` 标记高风险工具
  - 高风险工具请求会被阻止或标记为审批中
- 会话历史记录基础
  - `SESSION_HISTORY` 记录用户输入与工具执行结果

##  memory 模块目标

- 独立的记忆管理模块
  - 单独负责会话历史的存储、压缩、索引、检索和清空
  - 与主调度、工具执行、安全策略分离，避免记忆逻辑污染主业务流程
- 记忆分层设计
  - `memory/session_summary.json`：压缩记忆，保存用户目标、任务状态、关键结论和长期事实摘要
  - `memory/session_index.json`：索引层，记录时间、关键词、主题和候选事件入口，支持高效定位相关历史
  - `memory/session_history.json`：原始历史记忆，保留完整对话、工具执行记录和事实日志
- 记忆内容来源
  - 用户输入：需求、约束、偏好、历史问题
  - Agent 输出：建议、结论、状态总结、计划
  - 工具返回：CPU、内存、磁盘、服务、文件搜索等事实结果
  - 仅当信息对后续决策有长期价值时，才保留到长期记忆；不把所有临时中间日志都直接写入长期记忆
- 记忆生命周期与容量控制
  - 不应按“每次对话固定一份容量”简单处理，而应采用全局预算管理
  - 采用滑动窗口 + 摘要压缩 + 淘汰旧信息的策略控制总存储规模
  - 新增事件持续写入原始记录；超过预算时，将老记录压缩成摘要，并保留高价值事实
- 检索与 RAG 目标
  - 通过 `summary` 快速判断当前会话状态
  - 通过 `index` 定位相关事件与关键字命中位置
  - 通过 `history` 提供真实原始证据，给 RAG 提供最相关上下文
  - 让模型在回答时可同时访问“长程总结”和“短程证据”
- 调度顺序原则
  - 查询记忆时，优先读取 `session_summary.json` 作为总览
  - 再读取 `session_index.json` 找到候选相关事件
  - 最后读取 `session_history.json` 获取事实细节和证据
  - 若 `summary` 与 `history` 存在冲突，以 `history` 为最终事实依据；`summary` 只用于快速理解和高层判断
  - <span style="color:#888888">新增说明：记忆查询返回结果应包含 `summary`、`matched_history`、`index` 三部分，并在结果中明确说明是否存在 `summary`/`history` 冲突。</span>
- 逻辑分支
  - 先判断是否为记忆相关请求
  - 再区分“读取记忆”、“写入新记忆”、“更新压缩记忆”、“清空记忆”
  - 只读记忆请求可低风险处理；记忆改动、清空、重建摘要需更严格确认
- 设计原则
  - 原始记忆保真，压缩记忆抽象，索引提高召回效率
  - 记忆不等于所有系统日志；长期记忆必须有“价值筛选”
  - 记忆模块要区分事实、总结和当前上下文，避免把临时状态错误地当成长期能力
  - 多文档协同时需统一 `timestamp`、`session_id`、`event_id`，保证跨层检索的一致性和精准性
    - 多文档协同时需统一 `timestamp`、`session_id`、`event_id`，保证跨层检索的一致性和精准性

    - 记忆采用多层结构：原始历史、摘要、抽象、索引与向量索引
    - 关键词索引负责精确命中，向量索引负责语义召回
    - **抽象层（属于抽象）**：
      - 抽象层将多个相关事件聚合为一个语义对象，并通过 `abstract_id` 映射回 `event_ids`
      - 向量层优先对抽象文本做 embedding，以便语义检索
    - `retrieve()` 按 `summary -> keyword index -> vector index -> history` 的顺序检索
    - 语义召回不替代事实回溯，最终回答仍以原始历史为依据
    - 向量层用于提升自然语言查询与历史记忆之间的语义匹配能力
 - 记忆采用多层结构：原始历史、摘要、抽象、索引与向量索引
 - 关键词索引负责精确命中，向量索引负责语义召回（向量索引主要作用于抽象层）
 - 抽象层（Abstract）将多个相关事件聚合为一个语义对象，并通过 `abstract_id` 映射回 `event_ids`  （属于抽象层）
 - `retrieve()` 按 `summary -> keyword index -> vector index -> history` 的顺序检索
 - 语义召回不替代事实回溯，最终回答仍以原始历史为依据
 - 向量层用于提升自然语言查询与历史记忆之间的语义匹配能力（通常基于抽象文本的 embeddings）

## 设计原则

- 先保证低风险查询流程稳定
- 记忆模块单独管理，避免与工具调度混淆
- 记忆相关操作要显式分类，读写分开
- 由主 agent 负责意图识别与安全决策，记忆模块负责存取内容
